# RAG系统功能详解

## 1. RAG系统概述

RAG（Retrieval-Augmented Generation，检索增强生成）是一种结合信息检索和文本生成的技术。在舆情监测平台中，RAG系统允许用户基于已分析的舆情数据进行智能问答，提供了一个强大的交互式分析工具。

## 2. RAG核心组件

### 2.1 向量存储 (VectorStore)
- **位置**: `rag/vector_store.py`
- **功能**: 
  - 将舆情文章转换为向量并存储在ChromaDB中
  - 支持高效的语义检索
  - 管理文章元数据（情感标签、来源、时间等）
- **关键技术**: 
  - ChromaDB向量数据库
  - 嵌入向量生成
  - 元数据过滤

### 2.2 RAG引擎 (RAGEngine)
- **位置**: `rag/rag_engine.py`
- **功能**:
  - 构建基于检索结果的上下文
  - 生成针对用户查询的精准答案
  - 管理对话历史和引用标注
- **关键技术**:
  - 上下文构建
  - 提示词工程
  - 引用追踪

## 3. RAG工作流程

### 3.1 数据准备阶段
1. 舆情分析完成后，系统同时将文章存入SQLite数据库和ChromaDB向量库
2. 向量化过程将文章标题、摘要和内容组合后转换为固定维度的向量
3. 保留元数据如情感标签、来源URL、分析时间等

### 3.2 查询处理阶段
1. 用户输入问题后，系统首先将问题转换为向量表示
2. 在ChromaDB中搜索语义上最相似的舆情文章
3. 将检索到的文章作为上下文构建提示词
4. 使用大语言模型生成基于检索内容的答案
5. 标注答案中引用的原始文章来源

## 4. API接口详情

### 4.1 RAG聊天接口
- **路径**: `POST /api/rag/chat`
- **请求体**:
```json
{
  "message": "用户问题",
  "history": []  // 对话历史
}
```
- **响应**:
```json
{
  "response": "生成的回答",
  "sources": ["引用的文章ID列表"],
  "references": ["引用的具体文章信息"]
}
```

### 4.2 RAG统计接口
- **路径**: `GET /api/rag/stats`
- **响应**:
```json
{
  "total_documents": "向量库中文档总数",
  "indexed_articles": "已索引的文章数",
  "last_sync": "最后同步时间"
}
```

### 4.3 数据库同步接口
- **路径**: `POST /api/rag/sync`
- **功能**: 将SQLite数据库中所有文章同步到向量库
- **响应**:
```json
{
  "synced_count": "同步文章数量",
  "skipped_count": "跳过数量",
  "status": "同步状态"
}
```

## 5. 前端集成

### 5.1 智能助手页面
- 提供聊天界面进行RAG问答
- 显示知识库统计信息
- 支持手动同步数据库到向量库
- 展示引用来源和参考文献

### 5.2 引用展示
- 在答案中高亮引用的原始文章
- 提供跳转到原始分析结果的链接
- 显示检索到的相关文章列表

## 6. 性能优化策略

### 6.1 检索优化
- 使用相似度过滤减少无关结果
- 控制返回结果数量避免上下文过长
- 支持按情感标签等元数据过滤

### 6.2 上下文管理
- 限制上下文长度防止超出模型token限制
- 动态调整检索结果数量以平衡信息量和性能
- 智能缓存常用查询结果

## 7. 使用场景

### 7.1 舆情趋势分析
- "最近有哪些关于人工智能的负面新闻？"
- "过去一周科技行业的舆论变化如何？"

### 7.2 专题研究
- "关于某公司的评论分析汇总"
- "对比不同媒体对同一事件的报道态度"

### 7.3 快速查询
- "查找包含'政策'关键词的正面文章"
- "最近一个月的行业报道总结"

## 8. 技术优势

- **准确性**: 基于实际数据分析结果，避免幻觉
- **可追溯性**: 所有答案都有明确的引用来源
- **时效性**: 实时访问最新分析的舆情数据
- **交互性**: 支持多轮对话和深入探讨